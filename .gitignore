# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Build
node_modules/
dist/

# Secrets — never commit real credentials
.env
.env.*
!.env.example
*.pem
*.key
*service-account*.json
*credentials*.json

# Terraform — binaries, state, real values stay local
.terraform/
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.d/
*.tfvars
!*.tfvars.example
